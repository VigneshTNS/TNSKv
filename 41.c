#include<stdio.h>
int main()
{
int i,k;
char s[50];
printf(" Enter a string:");
scanf("%s",&s[i]);
printf("\n Enter number of times:");
scanf("%d",&k);
for(i=0;i<=k;i++)
{
printf("\n %s",s);
}
return 0;
}
