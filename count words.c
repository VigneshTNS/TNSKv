#include<stdio.h>
#include<string.h>
int main()
{
int i,count=0,d;
char a[100];
printf("please Enter different words:");
scanf("[\^n]%s",a[i]);
for(i=0;i<=a[i]='\0';i++)
  if(a[i]==' ')
  {
    count =count+1;
  }
  d=count+1;
  printf("\n number of words =%d",d);
  return 0;
}
