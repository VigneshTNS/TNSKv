#include<stdio.h>
#include<string.h>
int main()
{
int i,count=0,c,d;
char a[100];
printf("Enter different words:");
scanf("%s",&a[i]);
c=strlen(a);
for(i=0;i<=c;i++)
  if(a[i]=='32')
  {
    count =count+1;
  }
  d=count+1;
  printf("\n number of words =%d",d);
  return 0;
}
